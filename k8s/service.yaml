apiVersion: v1
kind: Service
metadata:
  name: fractal-core-service
  namespace: fractal-system
spec:
  selector:
    app: fractal-core
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: ClusterIP
